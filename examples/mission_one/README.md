# The Signal example

This complete 21-second plan uses one character and one environment. Empty asset directories are intentional placeholders for approved references; packaged references do not influence CogVideoX-2B text-to-video. `output/` receives manual Colab artifacts. Compile, inspect, export, run `colab/CINEOS-Mission-One.ipynb` on a compatible GPU, download the final film and JSON reports, then visually review action, camera, identity, continuity, and approximate lip-sync.
