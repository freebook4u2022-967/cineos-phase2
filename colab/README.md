# Mission One Colab

Open the notebook in Google Colab, select a T4 or compatible CUDA runtime, run setup, upload the exported ZIP, and execute the remaining cells. Runtime availability and model downloads are external hardware/network gates. The notebook reads the package; it contains no hard-coded production prompts or credentials.
