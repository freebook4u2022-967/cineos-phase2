# Validation example

Use any completed MP4 together with a conditioning package produced by
`cineos condition export`. See `docs/VALIDATION.md` for the complete commands.
The fake backend in `fake_backend.py` demonstrates deterministic plugin inputs.
