# First short-film example

Run `cineos film build project.json --renderer local-ai --output-dir output --dry-run` with an existing valid CINEOS project. The CLI intentionally does not fabricate assets or renderer output.
