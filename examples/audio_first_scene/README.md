# Audio first scene

Run `cineos audio plan project.json --output audio-project.json` here. Add an
explicitly rights-approved voice profile and assignment before synthesis. The
example deliberately does not invent a voice identity or include a recording.
