# Lip-sync metadata

Lip-sync tracks retain the dialogue cue and character identity, source audio hash, word and phoneme timing, derived visemes, mouth/jaw controls, confidence, offsets, and explicit fallback mode. Mapping is metadata-only: CINEOS does not train or synthesize a lip-sync model. Render adapters translate the neutral viseme vocabulary and report unsupported controls.
