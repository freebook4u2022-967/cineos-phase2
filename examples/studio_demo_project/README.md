# Studio demo project

Open `demo.cineos.json` from **File → Open project**. It contains one scene and
one shot and is intentionally renderer-independent. Validate and compile it before
configuring a local renderer.
