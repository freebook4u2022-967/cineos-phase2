# First real shot

Copy the renderer configuration to the working directory, compile a normal
CINEOS project, and export its ConditioningPackage. Always run `--dry-run`
before real inference. The example intentionally contains no weights or
fabricated reference-conditioning inputs; see `docs/RENDERER_SETUP.md`.
