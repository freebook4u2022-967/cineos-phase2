# First short film

Start with a valid project, run the film-build preflight, then connect an application renderer and validator to `FilmOrchestrator`. This milestone is an auditable integration baseline, not a claim of production or any particular commercial model's visual quality.

Keep `build.json` beside `shots/`. It is required for safe resume because selected outputs are verified against their recorded hashes.
